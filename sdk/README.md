Public VoiceOS SDK intent and hook helpers vendored from
`voiceos-integration-sdk/src/{intents,hooks}.ts` (MIT), for standalone builds and tests.
Runtime imports are bundled into `intentSdk.generated.js` by `bun run build-intents`.
When updating these helpers, copy both files from the same SDK revision.
